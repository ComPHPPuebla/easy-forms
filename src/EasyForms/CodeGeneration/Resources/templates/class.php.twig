<?php
namespace {{ form.formNamespace() }};

{% for class in form.elements() %}
use {{ class.getName() }};
{% endfor %}
use EasyForms\Form;

class {{ form.className() }} extends Form
{
    public function __construct()
    {
        $this
    {% for name, class in form.elements() %}
        ->add(new {{ class.getShortName() }}('{{ name }}', {% if class.hasChoices() %}[{% for value, label in class.choices() %}'{{ value }}' => '{{ label }}'{% if not loop.last %}, {% endif %}{% endfor %}]{% endif %}))
    {% endfor %}
    ;
    }
}
